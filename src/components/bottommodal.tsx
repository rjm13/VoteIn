import React, { useRef } from 'react';
import { View, Text, TouchableOpacity } from 'react-native';
import { Modalize } from 'react-native-modalize';

export const BottomModal = () => {

  return (
    <View>
        <Text>
            Test for the bottom modal.
        </Text>
    </View>
  );
};